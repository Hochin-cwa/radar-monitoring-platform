# 程式清單

以下表列執行此功能之相關程式與簡要說明，以提供系統維護人員維護與查詢使用。

- 系統/子系統代號：RMP
- 填寫人：
- 日期：114/07/20
- 系統/子系統名稱：遙測作業系統資料傳輸監控整合平台

## 表 52、作業程式清單

| 程式名稱 | 所在位置 | 程式語言 | 程式簡要說明 | 備註 |
|----------|----------|----------|--------------|------|
| main.py | backend/ | Python | FastAPI 應用程式進入點，註冊路由、掛載前端靜態檔、設定日誌 | |
| config.py | backend/ | Python | 從 config/config.yaml 載入所有設定（DB 連線、系統參數） | |
| database.py | backend/ | Python | SQLAlchemy 連線池管理，提供三個 MySQL 資料庫的 Session | |
| models.py | backend/ | Python | Pydantic 資料模型，定義所有 API 請求/回應格式 | |
| completeness.py | backend/routers/ | Python | /api/v1/completeness/current 端點，回傳所有儀器即時狀態 | |
| computers.py | backend/routers/ | Python | /api/v1/computers/current 端點，回傳電腦系統與磁碟狀態 | |
| history.py | backend/routers/ | Python | /api/v1/history/* 端點，回傳儀器與電腦歷史記錄 | |
| instruments.py | backend/routers/ | Python | /api/v1/instruments 端點，管理儀器清單與閾值設定 | |
| system.py | backend/routers/ | Python | /api/v1/system/current 及 /api/v1/disk/current 端點 | |
| alert_service.py | backend/services/ | Python | 儀器警示核心邏輯：查詢 FileCheck 表、計算時間差、閾值管理 | |
| history_service.py | backend/services/ | Python | 查詢儀器 DiffTime 及系統 CPU/記憶體/磁碟歷史資料 | |
| system_service.py | backend/services/ | Python | 查詢 SystemStatus 與 DiskStatus 資料庫，依科別分組 | |
| api.js | frontend/js/ | JavaScript | 統一封裝 fetch 呼叫，含 8 秒逾時與錯誤分類 | |
| clock.js | frontend/js/ | JavaScript | 共用時鐘模組，每秒更新本地時間與 UTC 時間 | |
| dashboard.js | frontend/js/ | JavaScript | 首頁儀表板控制器，顯示異常儀器及 Top 5 電腦指標 | |
| instruments.js | frontend/js/ | JavaScript | 儀器即時狀況頁面控制器，依科別分組並支援膠囊篩選 | |
| computers.js | frontend/js/ | JavaScript | 電腦即時狀況頁面控制器，統一卡片呈現系統與磁碟指標 | |
| history.js | frontend/js/ | JavaScript | 歷史資料頁面控制器，Chart.js 繪製時序折線圖 | |
| settings.js | frontend/js/ | JavaScript | 儀器閾值設定頁面控制器，可編輯表格與儲存功能 | |
| index.html | frontend/ | HTML | 首頁儀表板，四面板 grid 呈現異常儀器與電腦 Top 5 | |
| instruments.html | frontend/ | HTML | 儀器即時狀況頁面，含科別側欄與卡片 grid | |
| computers.html | frontend/ | HTML | 電腦即時狀況頁面，含科別側欄與統一卡片 | |
| history.html | frontend/ | HTML | 歷史資料頁面，含 DiffTime 圖與系統圖表 | |
| settings.html | frontend/ | HTML | 閾值設定頁面，含可編輯的儀器閾值表格 | |
| style.css | frontend/css/ | CSS | 全域樣式表，深色主題，支援 RWD（1920×1080 至 3840×2160） | |
| conftest.py | 根目錄 | Python | 測試根設定，將專案根目錄加入 sys.path | |
| test_api.py | tests/integration/ | Python | API 整合測試，使用 TestClient 模擬服務層 | |
| test_computers_api.py | tests/integration/ | Python | 電腦狀態 API 整合測試 | |
| test_db_access.py | tests/integration/ | Python | 真實資料庫連線整合測試（不可用時跳過） | |
| test_computer_status.py | tests/unit/ | Python | 前端檔案結構驗證單元測試 | |
| test_database.py | tests/unit/ | Python | database.py 單元測試：引擎初始化、驗證、重試邏輯 | |
| test_threshold_calculation.py | tests/property/ | Python | Hypothesis 屬性測試：閾值計算正確性（100 迭代） | |

---

# 檔案清單

## 表 53、作業相關檔案清單

| 檔案編號 | 檔案名稱 | 檔案位置 | 檔案說明 | 備註 |
|----------|----------|----------|----------|------|
| F-01 | config.yaml | config/ | 資料庫連線參數與系統設定 | 不納入版控 |
| F-02 | thresholds.yaml | config/ | 各儀器閾值設定（interval_minutes） | 執行期自動產生 |
| F-03 | requirements.txt | backend/ | Python 套件依賴清單 | |
| F-04 | pytest.ini | 根目錄 | Pytest 測試框架設定 | |
| F-05 | docker-compose.yaml | deploy/ | Docker Compose 服務定義（backend + nginx） | |
| F-06 | nginx.conf | deploy/ | Nginx 反向代理設定（前端靜態 + API proxy） | |
| F-07 | radar-monitor.service | deploy/ | systemd 服務單元檔 | |
| F-08 | run_uvicorn.sh | deploy/ | 啟動 uvicorn 後端伺服器腳本 | |
| F-09 | run_docker_nginx.sh | deploy/ | 啟動 nginx Docker 容器腳本 | |
| F-10 | .gitignore | 根目錄 | Git 忽略規則：config/、__pycache__/、logs/ | |
| F-11 | app.log | logs/ | 應用程式執行日誌（10MB 輪替，5 份備份） | 執行期產生 |
| F-12 | DSStatus_100_rows.sql | DBschema/ | DSStatus 資料表結構與 100 筆範例資料 | |
| F-13 | HFradarStatus_100_rows.sql | DBschema/ | HFradarStatus 資料表結構與 100 筆範例資料 | |
| F-14 | radarStatus_100_rows.sql | DBschema/ | radarStatus 資料表結構與 100 筆範例資料 | |
| F-15 | satelliteStatus_100_rows.sql | DBschema/ | satelliteStatus 資料表結構與 100 筆範例資料 | |
| F-16 | windprofilerStatus_100_rows.sql | DBschema/ | windprofilerStatus 資料表結構與 100 筆範例資料 | |

---

# 設定檔格式說明

- 系統/子系統代號：RMP
- 填寫人：
- 日期：114/07/20
- 系統/子系統名稱：遙測作業系統資料傳輸監控整合平台

---

## 檔案名稱：config.yaml

- 檔案組識：YAML 格式
- 產生頻率：一次
- 資料錄長度：不定長
- 更新頻率：不定（系統設定變更時）
- 保存期限：不定長

### 檔案說明

【用途】  
系統核心設定檔，定義資料庫連線參數與系統運作參數。

【檔案格式說明】

| 欄位 | 說明 |
|------|------|
| databases.file_status.host | FileStatus 資料庫主機位置 |
| databases.file_status.port | FileStatus 資料庫連接埠 |
| databases.file_status.name | FileStatus 資料庫名稱 |
| databases.file_status.user | FileStatus 資料庫使用者帳號 |
| databases.file_status.password | FileStatus 資料庫使用者密碼 |
| databases.file_status.pool_size | FileStatus 連線池大小 |
| databases.system_status.host | SystemStatus 資料庫主機位置 |
| databases.system_status.port | SystemStatus 資料庫連接埠 |
| databases.system_status.name | SystemStatus 資料庫名稱 |
| databases.system_status.user | SystemStatus 資料庫使用者帳號 |
| databases.system_status.password | SystemStatus 資料庫使用者密碼 |
| databases.system_status.pool_size | SystemStatus 連線池大小 |
| databases.disk_status.host | DiskStatus 資料庫主機位置 |
| databases.disk_status.port | DiskStatus 資料庫連接埠 |
| databases.disk_status.name | DiskStatus 資料庫名稱 |
| databases.disk_status.user | DiskStatus 資料庫使用者帳號 |
| databases.disk_status.password | DiskStatus 資料庫使用者密碼 |
| databases.disk_status.pool_size | DiskStatus 連線池大小 |
| system.query_timeout_seconds | 資料庫查詢逾時秒數 |
| system.reconnect_interval_seconds | 重連間隔秒數 |
| system.max_reconnect_attempts | 最大重連嘗試次數 |
| system.default_threshold_yellow | 預設黃色閾值（分鐘） |
| system.default_threshold_orange | 預設橙色閾值（分鐘） |
| system.default_threshold_red | 預設紅色閾值（分鐘） |

【內容範例】

```yaml
databases:
  file_status:
    host: localhost
    port: 3306
    name: FileStatus
    user: radar_ro
    password: ********
    pool_size: 5
  system_status:
    host: localhost
    port: 3306
    name: SystemStatus
    user: radar_ro
    password: ********
    pool_size: 5
  disk_status:
    host: localhost
    port: 3306
    name: DiskStatus
    user: radar_ro
    password: ********
    pool_size: 5

system:
  query_timeout_seconds: 10
  reconnect_interval_seconds: 5
  max_reconnect_attempts: 3
  default_threshold_yellow: 12.0
  default_threshold_orange: 17.0
  default_threshold_red: 27.0
```

---

## 檔案名稱：thresholds.yaml

- 檔案組識：YAML 格式
- 產生頻率：系統首次執行或使用者更新閾值時自動產生
- 資料錄長度：不定長
- 更新頻率：不定（使用者透過設定頁面更新時）
- 保存期限：不定長

### 檔案說明

【用途】  
各儀器的資料週期設定（interval_minutes），用於自動計算三段警示閾值。

【檔案格式說明】

| 欄位 | 說明 |
|------|------|
| defaults.interval_minutes | 預設資料週期 T（分鐘），未設定的儀器使用此值 |
| instruments.{FileType}.interval_minutes | 指定儀器的資料週期 T（分鐘） |

閾值自動計算規則：
- 黃色 = T + 5
- 橙色 = T + 10
- 紅色 = T + 20

【內容範例】

```yaml
defaults:
  interval_minutes: 7.0

instruments:
  HFradar_dt00:
    interval_minutes: 60.0
  windprofiler:
    interval_minutes: 30.0
  satellite:
    interval_minutes: 10.0
  DS_skewt:
    interval_minutes: 720.0
```

---

## 檔案名稱：docker-compose.yaml

- 檔案組識：YAML 格式
- 產生頻率：一次
- 資料錄長度：不定長
- 更新頻率：不定（部署架構變更時）
- 保存期限：不定長

### 檔案說明

【用途】  
Docker Compose 服務編排定義，包含後端（uvicorn）與前端（nginx）兩個容器。

【檔案格式說明】

| 欄位 | 說明 |
|------|------|
| services.backend.image | 後端 Docker 映像名稱 |
| services.backend.expose | 後端暴露連接埠（8000） |
| services.backend.volumes | 掛載設定檔與日誌目錄 |
| services.nginx.image | Nginx Docker 映像名稱 |
| services.nginx.ports | 對外映射連接埠（80:80） |
| services.nginx.volumes | 掛載 nginx.conf 與前端靜態檔目錄 |

【內容範例】

```yaml
version: '3'
services:
  backend:
    image: radar-backend:latest
    expose:
      - "8000"
    volumes:
      - /data/b615/radar-monitoring-platform/config:/app/config
      - /data/b615/radar-monitoring-platform/logs:/app/logs
    restart: unless-stopped

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - /data/b615/radar-monitoring-platform/deploy/nginx.conf:/etc/nginx/conf.d/default.conf
      - /data/b615/radar-monitoring-platform/frontend:/usr/share/nginx/html
    depends_on:
      - backend
    restart: unless-stopped
```

---

## 檔案名稱：radar-monitor.service

- 檔案組識：Systemd Unit 格式
- 產生頻率：一次
- 資料錄長度：不定長
- 更新頻率：不定
- 保存期限：不定長

### 檔案說明

【用途】  
Linux systemd 服務單元檔，用於管理後端 uvicorn 程序的啟動、停止與自動重啟。

【檔案格式說明】

| 欄位 | 說明 |
|------|------|
| Unit.Description | 服務描述 |
| Unit.After | 啟動順序依賴 |
| Service.Type | 程序類型（simple） |
| Service.User | 執行身份（radar） |
| Service.WorkingDirectory | 工作目錄 |
| Service.ExecStart | 啟動指令 |
| Service.Restart | 重啟策略（on-failure） |
| Install.WantedBy | 安裝目標 |

【內容範例】

```ini
[Unit]
Description=Radar Monitoring Platform
After=network.target

[Service]
Type=simple
User=radar
WorkingDirectory=/opt/radar-monitoring-platform
ExecStart=/opt/radar-monitoring-platform/venv/bin/uvicorn backend.main:app --host 0.0.0.0 --port 8000
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```
